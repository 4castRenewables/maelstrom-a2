# ONLY USED FOR SHOWCASING MANTIK
entry_points:
  split:
    # parameters:
    #   select_relevant:
    #     type: str
    #     default: False
    command: >
      python build_dataset_rain_classifier.py
        --select_relevant {select_relevant}
  train:
    parameters:
      batch_size:
        type: int
        default: 32

    command: >
      python finetune_deberta_classifier.py
        --batch_size {batch_size}
